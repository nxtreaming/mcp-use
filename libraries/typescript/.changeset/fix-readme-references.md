---
"mcp-use": patch
"@mcp-use/cli": patch
"@mcp-use/inspector": patch
"create-mcp-use-app": patch
---

Fix stale mcp-use-ts references in README badges, image URLs, and eslint config to point to the new mcp-use monorepo
